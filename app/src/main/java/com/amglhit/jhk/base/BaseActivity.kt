package com.amglhit.jhk.base

import android.Manifest
import android.support.v7.app.AppCompatActivity
import com.amglhit.msuite.toast
import permissions.dispatcher.*


abstract class BaseActivity : AppCompatActivity() {

//  override fun onCreate(savedInstanceState: Bundle?) {
//    super.onCreate(savedInstanceState)
//    val lightStatusBar: Boolean = true
//    val isLightMode =
//      setStatusBarLightMode(color(R.color.status_bar), lightStatusBar)
//    if (!isLightMode) {
//      setStatusBarColor(color(R.color.status_bar_dark))
//    }
//    Timber.d("support light status: $isLightMode, is light: $lightStatusBar")
//  }
}